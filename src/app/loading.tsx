// loading.tsx
import React from 'react';

const Loading = () => {
  return (
    <div className="loading-container">
      <div className="loading-spinner">
        {/* You can use a simple CSS animation or an image for the spinner */}
        Loading...
      </div>
    </div>
  );
};

export default Loading;
